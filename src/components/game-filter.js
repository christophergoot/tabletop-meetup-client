import React from 'react';

export default function GameFilter(props) {
    return (
		<div>Filter
			<span className="sort">players</span>
			<span className="sort">play time</span>
			<span className="sort">rating</span>
			<span className="sort">weight</span>
		</div>
	);
};